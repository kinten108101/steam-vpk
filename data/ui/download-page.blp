using Gtk 4.0;
using Adw 1;

template DownloadPage : Gtk.Box {
  orientation: vertical;

  Gtk.ScrolledWindow {
    Adw.Clamp {
      Gtk.ListBox list_box {
        selection-mode: none;
        valign: start;
        styles ["boxed-list"]
      }
    }
  }
}
